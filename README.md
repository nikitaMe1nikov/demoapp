# Demoapp = NextJS + React + Styled-Components + Redux

## Install dependencies

Run `npm install` or `yarn install`

## Start dev server

Run `npm run dev` or `yarn dev` and open browser localhost:8000